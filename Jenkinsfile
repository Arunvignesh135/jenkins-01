pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                echo "Code checkout completed"
            }
        }

        stage('Build') {
            steps {
                echo "Build stage completed successfully team"
            }
        }
    }
}
